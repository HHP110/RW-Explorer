<Page x:Class="RW_Explorer.Controls.RouteManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="线路管理"
      Background="#FF1E1E1E">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 顶部搜索栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="10" VerticalAlignment="Center">
            <TextBox x:Name="txtSearch" Width="300" Margin="0,0,10,0"
                     VerticalContentAlignment="Center" Background="#FF252526"
                     Foreground="White" BorderBrush="#FF3E3E3E"
                     KeyDown="txtSearch_KeyDown"
                     TextChanged="txtSearch_TextChanged">
                <TextBox.InputBindings>
                    <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <Button Content="搜索" Style="{StaticResource ModernButton}" 
                    Click="SearchRoutes_Click" Width="80"/>
        </StackPanel>

        <!-- 内容区域 -->
        <Grid Grid.Row="1">
            <Frame x:Name="mainFrame" NavigationUIVisibility="Hidden"
                   Background="#FF1E1E1E" BorderThickness="0"/>

            <!-- 无结果提示 -->
            <TextBlock x:Name="txtNoResults" Text="没有找到匹配的线路" 
                       Foreground="#FFAAAAAA" FontSize="16" FontStyle="Italic"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       Visibility="Collapsed"/>
        </Grid>
    </Grid>
</Page>