<Window x:Class="RW_Explorer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:RW_Explorer"
        mc:Ignorable="d"
        Title="Path Settings" Height="310" Width="320" WindowStartupLocation="CenterScreen" Icon="/icon.png">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="当前游戏目录:" Margin="0,0,0,5"/>
        <TextBox x:Name="txtGameDirectory" Grid.Row="1" IsReadOnly="True" Margin="0,0,0,10"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="选择游戏目录" 
                    Click="BrowseButton_Click" 
                    Padding="10,5" Margin="0,0,10,0"/>

            <Button x:Name="RunButton" 
                    Content="启动" 
                    Click="RunButton_Click" 
                    Padding="10,5"
                    IsEnabled="False"/>
        </StackPanel>
    </Grid>
</Window>